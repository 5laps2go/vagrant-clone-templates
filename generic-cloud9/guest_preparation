curl -L get.rvm.io | bash
source /home/vagrant/.bashrc
rvm install 2.6.3
rvm --default use 2.6.3
# nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.35.2/install.sh | bash
source /home/vagrant/.bashrc
nvm install v10.23.0
npm install -g yarn; yarn install

# install .ssh/id_rsa
touch ~/.ssh/id_rsa; chmod 600 ~/.ssh/id_rsa; vi ~/.ssh/id_rsa
git clone git@github.com:5laps2go/rails-tutorial.git
cd rails-tutorial/ch15
bundle install
#bundle update --bundler   # Gemfile.lockのBUNDLED WITHを現bundlerに更新
bundle config disable_platform_warnings true

rails webpacker:install
rails db:migrate
# edit Gemfile to update bcrypt 3.1.11 to 3.1.12 for ch1 - ch14
# bundle install
# gem uninstall bcrypt:3.1.11

rails test

git config --global user.email "hiroaki.etoh@gmail.com"
git config --global user.name "Hiroaki Eto"

